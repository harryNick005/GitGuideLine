1. git init (Create local repository)
2. git add (path to file)
3.git commit -m"Message" (Message part would be shown in remote repository to remind what you pushed)
	-- git branch -M main (main is the name of your local branch)
         (use git branch) check your local branch (use git branch -r) check your remote branch
 	
	--git remote add <name> <URL>
	(use git remote or git remote -v)to check your remote repository


4.git push <name> <branchname>